[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = ['npm install --production=false']

[phases.build]
cmds = ['echo "Build completed successfully"']

[start]
cmd = 'node app.js'

[variables]
NODE_ENV = 'production'